name: np_ui
description: Generic UI widgets and theming support package
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  np_common:
    path: ../np_common

dev_dependencies:
  np_lints:
    path: ../np_lints
