name: np_ui
description: Generic UI widgets and theming support package
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.11.0"
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  np_common:
    path: ../np_common

dev_dependencies:
  dart_style: 3.1.2
  np_lints:
    path: ../np_lints
