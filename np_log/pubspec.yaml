name: np_log
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.11.0"
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.2.0
  np_log_annotation:
    path: ../np_log_annotation
  np_platform_util:
    path: ../np_platform_util

dev_dependencies:
  dart_style: 3.1.2
  np_lints:
    path: ../np_lints
